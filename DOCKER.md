# XSB Duplicate - Telegram Bot with Docker

Telegram бот для управления задачами с поддержкой Docker и PostgreSQL.

## Требования

- Docker 20.10+
- Docker Compose 2.0+

## Быстрый старт

### 1. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и отредактируйте:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
BOT_TOKEN=YOUR_ACTUAL_BOT_TOKEN
OWNER_ID=YOUR_TELEGRAM_USER_ID
DB_PASSWORD=your_secure_password
```

### 2. Запуск контейнеров

```bash
docker-compose up -d
```

Первый запуск может занять несколько минут. Проверьте логи:

```bash
docker-compose logs -f bot
```

### 3. Остановка сервиса

```bash
docker-compose down
```

## Основные команды Docker Compose

```bash
# Запуск в фоне
docker-compose up -d

# Запуск с пересборкой образа
docker-compose up --build -d

# Просмотр логов бота
docker-compose logs -f bot

# Просмотр логов БД
docker-compose logs -f db

# Просмотр всех логов
docker-compose logs -f

# Статус контейнеров
docker-compose ps

# Перезагрузка контейнеров
docker-compose restart

# Остановка (контейнеры сохраняются)
docker-compose stop

# Запуск остановленных контейнеров
docker-compose start

# Полное отключение (контейнеры удаляются)
docker-compose down

# Удаление всех данных (включая БД)
docker-compose down -v
```

## Структура сервисов

### 🗄️ Database (PostgreSQL 15)

- **Хост**: `db:5432`
- **Контейнер**: `xsb_db`
- **Порт на хосте**: `5432`
- **Объем**: `postgres_data` (сохранение данных)
- **Статус проверки**: healthcheck каждые 10 секунд

### 🤖 Bot (Python 3.11)

- **Контейнер**: `xsb_bot`
- **Объемы**:
  - Синхронизация кода (для разработки)
  - `bot_persistence_data` (сохранение сессий бота)
- **Зависимость**: Автоматически ждет готовности БД

## Переменные окружения

| Переменная    | Описание            | По умолчанию |
| ------------- | ------------------- | ------------ |
| `BOT_TOKEN`   | Токен Telegram бота | Обязательна  |
| `OWNER_ID`    | ID владельца бота   | Обязательна  |
| `DB_HOST`     | Хост БД             | `db`         |
| `DB_PORT`     | Порт БД             | `5432`       |
| `DB_USER`     | Пользователь БД     | `postgres`   |
| `DB_PASSWORD` | Пароль БД           | `postgres`   |
| `DB_NAME`     | Имя БД              | `xsb_db`     |

## Разработка

### ✏️ Горячая перезагрузка

Код синхронизируется в реальном времени из локальной папки в контейнер. Измените файлы локально и перезагрузите приложение:

```bash
docker-compose restart bot
```

### 🔌 Доступ к БД из хоста

Подключитесь к PostgreSQL локально:

```bash
psql -h localhost -U postgres -d xsb_db
```

Или используйте графический клиент (DBeaver, pgAdmin и т.д.):

- Хост: `localhost`
- Порт: `5432`
- Пользователь: `postgres`
- БД: `xsb_db`

### 📋 Просмотр логов приложения

```bash
# Последние 100 строк
docker-compose logs bot

# Постоянный просмотр логов
docker-compose logs -f bot

# Последние 50 строк с меток времени
docker-compose logs --tail=50 -t bot
```

### 🐚 Выполнение команд в контейнере

```bash
# Интерактивная оболочка Python
docker-compose exec bot python

# Выполнение команды
docker-compose exec bot python -c "print('Hello')"

# Bash оболочка
docker-compose exec bot /bin/bash
```

## Проблемы и решения

### 🔧 Бот не подключается к БД

```bash
# Проверьте статус всех сервисов
docker-compose ps

# Проверьте логи БД
docker-compose logs db

# Убедитесь, что БД готова (healthcheck)
docker-compose logs db | grep healthcheck

# Перезагрузитесь
docker-compose down
docker-compose up -d
```

### 📦 Требуется пересборка контейнера

После изменения `requirements.txt`:

```bash
docker-compose up --build -d
```

### 🗑️ Полный сброс (будут удалены все данные!)

```bash
docker-compose down -v
docker-compose up -d
```

### ⚠️ Порт уже используется

Если порт 5432 занят другим процессом:

```bash
# Измените порт в docker-compose.yml
# "5433:5432" вместо "5432:5432"
# или остановите процесс использующий порт
```

### 🔴 Бот не запускается

Проверьте переменные окружения:

```bash
# Убедитесь, что .env файл существует
ls -la .env

# Проверьте значения
cat .env

# Просмотрите логи
docker-compose logs bot
```

## Безопасность

- ⚠️ Используйте надежные пароли в `.env`
- 🔒 Не коммитьте `.env` в git (добавлено в `.gitignore`)
- 🔄 Обновляйте образы регулярно: `docker-compose pull`
- 🔑 Используйте переменные окружения для чувствительных данных

## Продакшн развертывание

Для продакшна рекомендуется:

1. Использовать переменные окружения вместо `.env` файла
2. Использовать именованные объемы с регулярным резервным копированием
3. Настроить мониторинг и централизованное логирование
4. Использовать reverse proxy (nginx) перед приложением
5. Настроить SSL сертификаты (Let's Encrypt)
6. Использовать специальный пользователь (не root) в контейнере
7. Настроить resource limits (CPU, Memory)
8. Использовать private Docker registry для образов

## Полезные ссылки

- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [PostgreSQL Docker Hub](https://hub.docker.com/_/postgres)
- [Python Docker Hub](https://hub.docker.com/_/python)
